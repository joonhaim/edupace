// ECG engine intentionally disabled to allow a fresh rebuild from scratch.
// Frontend training UI (canvas, overlays, settings) stays in place.
function initEcgEngine() {
    // No-op: ECG backend logic removed.
}

export { initEcgEngine };
